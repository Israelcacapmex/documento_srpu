version: "3.3"

services:
  py-pdf3:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9091:7000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - Variable_entorno=
